export interface ProjetoCorporativoModel {
    id: number | null,
    nome: string,
    codigoProjeto: number | null,
    custoEstimado: number | null,
    prazoMeses: number | null,
    gerente: string
  }
